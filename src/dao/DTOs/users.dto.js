



export  class CurrentUserDTO {
    constructor({ email,role, first_name,last_name,age,cart }) {
        this.user = `${first_name}`|| email
        this.email = email
        this.role=role
      
       
    }
}